- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.irssi/config:
        path: irssi/config
        create: true
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
